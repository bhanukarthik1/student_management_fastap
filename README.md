# student_management_fastap
A backend Student Management System built using Python and SQL that allows administrators to manage student profiles, courses, and academic records. The system supports create, read, update, and delete operations, ensures data consistency using relational database design, and focuses on clean backend logic without using external frameworks.
